import React from "react";
// import PropTypes from "prop-types";

const Board = (props) => {
    console.log(props);
    return (
        <div className="board">
            asd
        </div>
    );
}

export default Board;